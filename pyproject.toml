[build-system]
requires = ["setuptools>=83.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "1200km-aidebug"
version = "3.1.0"
description = "Malware triage, Ghidra reconstruction, active ELF debugging, and live assembly learning."
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [{ name = "Andrey Pautov", email = "1200km@gmail.com" }]
keywords = [
  "malware-analysis",
  "reverse-engineering",
  "debugger",
  "frida",
  "capstone",
  "yara",
  "mitre-attack",
  "cti",
]
classifiers = [
  "Development Status :: 4 - Beta",
  "Environment :: Console",
  "Intended Audience :: Information Technology",
  "Intended Audience :: Science/Research",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Security",
]
dependencies = [
  "asn1crypto>=1.5.1",
  "capstone>=5",
  "cryptography>=43",
  "pefile>=2023.2.7",
  "pyelftools>=0.31",
  "python-dotenv>=1.0.1",
  "rich>=15.0.0",
  "textual>=8.2.8",
]

[project.optional-dependencies]
ai = ["anthropic>=0.120.2", "openai>=2.0.0", "yara-python>=4.5.4"]
dynamic = ["frida>=17.17.0,<18"]
all = ["anthropic>=0.120.2", "frida>=17.17.0,<18", "openai>=2.0.0", "yara-python>=4.5.4"]
dev = [
  "anthropic>=0.120.2",
  "bandit>=1.9.4",
  "build>=1.5.0",
  "packaging>=24",
  "pip-audit>=2.10.1",
  "openai>=2.0.0",
  "pytest>=8",
  "pytest-cov>=5",
  "ruff>=0.16.1",
  "tomli>=2; python_version < '3.11'",
  "twine>=5",
  "yara-python>=4.5.4",
]

[project.scripts]
aidebug = "main:main"

[project.urls]
Homepage = "https://github.com/anpa1200/AIDebug"
Repository = "https://github.com/anpa1200/AIDebug"
Documentation = "https://github.com/anpa1200/AIDebug#readme"
Issues = "https://github.com/anpa1200/AIDebug/issues"
Changelog = "https://github.com/anpa1200/AIDebug/blob/main/CHANGELOG.md"
Security = "https://github.com/anpa1200/AIDebug/blob/main/SECURITY.md"

[tool.setuptools]
py-modules = ["main", "config"]
include-package-data = true

[tool.setuptools.packages.find]
include = ["analysis*", "debugger*", "learning*", "reporting*", "storage*", "ui*"]

[tool.setuptools.package-data]
analysis = ["data/*.json", "data/*.java", "data/*.md"]
debugger = ["scripts/*.js"]
learning = ["cases/*.c", "cases/*.h", "cases/*.json", "cases/*.md"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 110
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
ignore = ["E501"]
